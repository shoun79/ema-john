/**
 * create a firebase
 * create web app
 * npm i firebase
 * save firebase config and export default app
 * build> authentication>Get started>enable sign in methods
 * create sign up and login routes
 */


/**
 * create a context provider file
 * create a context and set provider
 * set the children props
 * set context value
 * set the provider
 * 
 */

/**
 * Hosting
 * --------------------
 * One time per pc 
 * 1.npm install -g firebase-tools
 * 2.firebase login
 *
 * 
 * for each project one time
 * 1.firebase init
 * 2.proceed 
 * 3.hosting firebase (down arrow) use space bar to select
 * 4.existing project
 * 5.seleted project
 * 6.which project as public directory: dist (if react app build)
 * 7.single page application:yes
 * 8.continuos deployment:no
 * 
 * for every time deploy
 * npm run build
 * firebase deploy
 */